# Q11 API Retry Handler

Retry mechanism for failed API requests.

## Features

- Makes a mock API request.
- Automatically retries failed requests up to 3 times.
- Shows each attempt in the UI.
- Displays a success message when a retry recovers.
- Displays a final error message when all retries fail.

## How to Run

Open `index.html` directly in a browser.
